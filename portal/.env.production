# The portal is served from ckpsca.in but the API lives on api.ckpsca.in,
# so in production the portal calls the API by its absolute URL.
# (In development this is ignored — Vite proxies /api to localhost:8000.)
VITE_API_BASE=https://api.ckpsca.in/api
